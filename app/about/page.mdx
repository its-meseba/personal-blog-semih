import { aboutData } from "./about-data";
import { PersonalHome, BackgroundSection } from "./components/personal-home";
import { EducationSection } from "./components/education-section";
import { AchievementsSection } from "./components/achievements-section";
import { LatestPostsSection } from "./components/latest-posts-section";
import { JsonLd } from "../components/JsonLd";
import { personSchema, websiteSchema } from "../structured-data";
import { OG_DEFAULT_IMAGE, SITE_DESCRIPTION, SITE_TITLE, SITE_URL } from "../author";
import { atomAlternateTypes } from "@/lib/feed-links";

export const ABOUT_URL = SITE_URL + '/about'

export const metadata = {
  title: { absolute: SITE_TITLE },
  description: SITE_DESCRIPTION,
  alternates: {
    canonical: ABOUT_URL,
    types: atomAlternateTypes(),
  },
  openGraph: {
    type: 'profile',
    url: ABOUT_URL,
    title: SITE_TITLE,
    description: SITE_DESCRIPTION,
    images: [{ url: OG_DEFAULT_IMAGE, width: 1200, height: 630, alt: SITE_TITLE }]
  },
  twitter: {
    card: 'summary_large_image',
    title: SITE_TITLE,
    description: SITE_DESCRIPTION,
    images: [OG_DEFAULT_IMAGE]
  }
}

<JsonLd data={personSchema()} />
<JsonLd data={websiteSchema()} />

<PersonalHome>
  <LatestPostsSection />
  <BackgroundSection>
    <details>
      <summary>Education &amp; research</summary>
      <EducationSection title={aboutData.education.title} degrees={aboutData.education.degrees} research={aboutData.education.research} />
    </details>
    <details>
      <summary>Achievements &amp; recognition</summary>
      <AchievementsSection title={aboutData.achievements.title} items={aboutData.achievements.items} />
    </details>
  </BackgroundSection>
</PersonalHome>
