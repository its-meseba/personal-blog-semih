import { aboutData } from "./about-data";
import { AboutLayout } from "./components/about-layout";
import { IntroSection } from "./components/intro-section";
import { ProjectsSection } from "./components/projects-section";
import { ExperienceSection } from "./components/experience-section";
import { EducationSection } from "./components/education-section";
import { AchievementsSection } from "./components/achievements-section";
import { LatestPostsSection } from "./components/latest-posts-section";
import { JsonLd } from "../components/JsonLd";
import { personSchema, websiteSchema } from "../structured-data";
import { OG_DEFAULT_IMAGE, SITE_DESCRIPTION, SITE_TITLE, SITE_URL } from "../author";
import { atomAlternateTypes } from "@/lib/feed-links";

export const ABOUT_URL = SITE_URL + '/about'

export const metadata = {
  title: { absolute: SITE_TITLE },
  description: SITE_DESCRIPTION,
  alternates: {
    canonical: ABOUT_URL,
    types: atomAlternateTypes(),
  },
  openGraph: {
    type: 'profile',
    url: ABOUT_URL,
    title: SITE_TITLE,
    description: SITE_DESCRIPTION,
    images: [{ url: OG_DEFAULT_IMAGE, width: 1200, height: 630, alt: SITE_TITLE }]
  },
  twitter: {
    card: 'summary_large_image',
    title: SITE_TITLE,
    description: SITE_DESCRIPTION,
    images: [OG_DEFAULT_IMAGE]
  }
}

<JsonLd data={personSchema()} />
<JsonLd data={websiteSchema()} />

<AboutLayout>
  <div className="pb-chapter">
    <IntroSection
      title={aboutData.intro.title}
      description={aboutData.intro.description}
      additionalInfo={aboutData.intro.additionalInfo}
      personalNote={aboutData.intro.personalNote}
    />

    <LatestPostsSection />

    <ProjectsSection
      title={aboutData.featuredProjects.title}
      projects={aboutData.featuredProjects.projects}
    />

    <ExperienceSection
      title={aboutData.experience.title}
      positions={aboutData.experience.positions}
    />

    <EducationSection
      title={aboutData.education.title}
      degrees={aboutData.education.degrees}
      research={aboutData.education.research}
    />

    <AchievementsSection
      title={aboutData.achievements.title}
      items={aboutData.achievements.items}
    />
  </div>
</AboutLayout>
